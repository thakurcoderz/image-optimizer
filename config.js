// config.js
const config = {
  inputDir: 'input', // Change to your source folder 212
  outputDir: 'output', // Change to your destination folder
  maxWidth: 1000, // Example: Resize width
  maxHeight: null, // Example: Resize height
  allowedFormats: ['jpg', 'jpeg', 'png', 'webp', 'tiff'], // Define allowed image formats
}

module.exports = config;